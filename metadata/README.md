# metadata

Project-level metadata files can be placed here, including sample metadata, publication source tables, tissue classifications, and library exclusion records.

The migrated raw-data example workflow includes:

```text
example_miRNA_metadata.tsv
example_degradome_metadata.tsv
```

These files document the representative sRNA-seq and degradome libraries used
by the example miRNA discovery and target-prediction workflow.
